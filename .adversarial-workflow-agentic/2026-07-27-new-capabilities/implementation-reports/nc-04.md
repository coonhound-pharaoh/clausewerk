# NC-04 — Implementation report

**Package:** Prepare the draft-record design session (D-4). Document only, no code.

## Files created

- `docs/D4-DRAFT-RECORD-SESSION-PREP-2026-07-27.md` — the session-prep sheet. NC-04 assigns no
  exact path ("exact path assigned at package start"), so the package brief's default was used.
- `.adversarial-workflow-agentic/2026-07-27-new-capabilities/implementation-reports/nc-04.md` — this report.

No source file was modified. No migration written.

## Sources verified at source before writing

- `backend/db/migrations/0008_review_queue.sql` — `cw.clause_draft` (56–75), `cw.review_ticket`
  (82–143), immutability of the proposal half (261–277), no reopen/redecide (279–284), derived
  `edited_before_approval` assigned over caller input (292–301), delete raises (311–333), draft
  frozen when used + no delete + no truncate (361–395).
- `backend/db/migrations/0012_sow_override.sql:295–305` — U4: `unedited_approval_threshold` settled
  as deliberately unset; the system must never choose the number.
- `backend/db/migrations/0023_redaction_and_purge.sql:318–395` — purge reaches the signed-contract
  records only (signature certificates, executed signatories, executed documents, executed
  agreement). It does not reach the AI-use record.
- `.adversarial-workflow-agentic/2026-07-27-new-capabilities/new-capabilities-plan.html` — WP-6
  (similarity computed by the database at approval, stored with the draft's record, thresholds as
  governance settings), WP-7 (three EU documentation fields; "three new fields, not a new record"),
  decision D-4 (one session, before the first database change touching the draft record).

## Acceptance criteria

| Criterion | Result |
|---|---|
| Names every existing `cw.clause_draft` column and every `cw.review_ticket` column, cited to 0008 | PASS — §2a lists all 10 columns plus the not-blank rule; §2b lists all 19 columns |
| Lists exactly four candidate new fields, states they serve both consumers; a fifth is flagged as scope growth | PASS — §5 table F1–F4, explicit both-consumers statement, explicit fifth-field warning, plus decision line 5.1 |
| For each field: table, nullability and writer as open questions with options, never a number or wording | PASS — §5 poses the three questions; §6 gives three lettered questions per field with options and a recommendation of placement/nullability/writer only |
| Append-only constraint stated in plain business language with citations in a footer | PASS — §3 in plain language; all citations collected in the footer |
| No proposed threshold value | PASS — §4 states no number is proposed and the system must never pick one; no numeric threshold appears |
| `git status --short backend/` unchanged by this package | PASS — this package wrote nothing under `backend/`. The one entry present (`?? backend/engine/ladder_placement.py`) belongs to NC-06 and predates this work |

## Deviations

- None from scope. One judgement call: the sheet adds a short factual note that today's
  customer-data deletion does not reach the AI-use record, sourced from 0023 as the brief
  instructed, and explicitly marks the "should it?" question as D-6 and out of this session.
- Recommendations are given for each question, as the brief requires, but only on placement,
  nullability and writer — never a number and never wording.
