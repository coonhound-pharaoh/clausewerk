# Status — defect remediation, 2026-07-28

**Short version:** the work is finished and tested. **None of it is committed or pushed.**
It exists only as uncommitted edits on this machine.

---

## Is it pushed?

**No.** `origin/main` and local `main` are identical — 0 ahead, 0 behind. That is because a
*second* session has been committing and pushing to `main` all day. None of those commits are
this work.

Everything below is uncommitted. If this working directory is lost, the work is lost. There is
no branch and no stash holding it.

---

## Finished

Eleven of the thirteen planned packages. Every fix that could be demonstrated failing without
it, was.

| Defect | What it was | Status |
|---|---|---|
| **B1** | A caller's typo reported the service as broken, on every write endpoint | Closed |
| **B2** | A slow AI provider held a database connection and could stop the whole product | Closed |
| **B3** | Reported as a bug; wasn't one | Withdrawn — wording corrected, no code |
| **B4** | Five tables holding run evidence have no row-level security | Review tripwire added |
| **B5** | A refusal path could crash into a service error | Closed |
| **B6** | Three audit writers built SQL by string-joining | Closed |
| **B7** | Two field rules cancelled each other instead of combining | Closed |
| **B8** | A comment explained behaviour that had changed | Closed |
| **B9** | Two test runs at once collided and produced mystery failures | Closed |
| **B10** | The verification gate was red, silently — nothing was being checked | Closed |
| **A-1** | The supplier-facing account could read every live session key | Closed |

**Evidence**

- Doorway tests: **723 passed**, no failures, no errors (was 566 passed + 1 error)
- Engine tests: **202 passed**, unchanged
- Verification gate: **exit 0**, 34 of 35 guarantees caught (had been aborting before it ran
  anything)
- No existing test was weakened, skipped or deleted

**Decisions recorded** in `memory.md` as **S96–S106**, each with its reasoning.

---

## Open

**B11 — one check guards nothing.** Found only because repairing the gate (B10) made it
visible for the first time. Removing the code it protects does not fail the test that names
it. Proven to pre-date this work, not caused by it. Needs a decision: give it something real
to check, or retire it with a written reason. *This is the only defect still open.*

**No SQL-side test covers the session table.** The A-1 protection is guarded by a Python test
only. Worth closing; not urgent.

**The other verification harness has never been checked for the same rot.** `npm run verify`
runs a SQL harness *before* the Python one. The Python one turned out to be silently red, and
nobody has looked at the SQL one. Worth an hour — the answer tells you whether the database
guarantees are being evaluated at all.

**Deferred by plan, unchanged:** real row-level security on the five run-evidence tables
(large, with real blast radius), and a checksum column on the migration ledger.

---

## What is in the working tree, and whose it is

Not everything uncommitted here is mine. This matters before anything is committed.

**Mine — this work**

```
backend/doorway/advisory.py        backend/doorway/refusals.py
backend/doorway/executions.py      backend/doorway/runs.py
backend/doorway/identity.py        backend/doorway/setup.py
backend/doorway/manifests.py       backend/doorway/writes.py
backend/doorway/migrate.py         backend/engine/manifest.py
backend/doorway/mutation_check.py  backend/doorway/test_writes.py
memory.md

new files:
backend/db/migrations/0033_the_session_table_answers_only_to_sign_in.sql
backend/doorway/test_concurrent_prepare.py
backend/doorway/test_session_privacy.py
backend/doorway/test_unprotected_tables.py
REVISION-PLAN-2026-07-28.md
.adversarial-workflow-agentic/2026-07-28-defect-remediation/
```

**Mixed — mine on top of the other session's unfinished Phase 2 work**

```
backend/doorway/app.py        (their session work + my corrected comment)
backend/doorway/sessions.py   (their session work + my import fixes and constants)
```

**Not mine — the other session's, still unfinished**

```
backend/doorway/server.py                     backend/doorway/test_retirement.py
backend/doorway/test_identity.py              backend/doorway/test_sessions.py
backend/db/migrations/0032_session_store.sql  prototype/v4/index.html
prototype/v4/app/parchment.css                .claude/
```

**Inputs, pre-existing:** `bug_report.md`, `audit_results.md`.

---

## Before committing — read this

**Two sessions are writing this repository at once.** That is not a theory; it happened
repeatedly today. The other session claimed two `memory.md` entry numbers while this work was
in flight, which I renumbered at merge. It also committed changes to migration files that had
already been applied.

Consequences for whoever commits:

1. **`app.py` and `sessions.py` carry two people's unfinished work.** Committing them commits
   the other session's Phase 2 changes too. That may be fine — but it should be a decision,
   not a surprise.
2. **`0032` is untracked and still being revised by the other session.** My `0033` depends on
   it existing and is written defensively so it applies either way, but `0032` must land
   first.
3. **`memory.md` numbering will collide again** if both sessions append before this is
   committed. Check the highest number in the file, not the last one in it — they are not the
   same, and that mistake was made once already today.

**Recommendation:** commit this work on its own branch rather than straight onto `main`, so
the Phase 2 work and this remediation can be reviewed apart from each other.

---

## Rerunning the checks

```bash
cd backend && python -m pytest engine -q && python -m pytest doorway -q
```

```bash
cd backend && python doorway/mutation_check.py
```

The doorway suite takes about 15 minutes. Do not run two at once — that was B9, and the fix
makes it survivable, not free.
