{
  "name": "clausewerk-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Clausewerk backend — deterministic core. Clause registry, resolution, audit.",
  "type": "module",
  "scripts": {
    "test": "node db/test/run-all.mjs",
    "test:mutation": "node db/test/mutation-check.mjs && python engine/mutation_check.py",
    "verify": "npm run test && npm run test:engine && npm run test:mutation",
    "test:engine": "python -m pytest engine -q"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@electric-sql/pglite": "^0.5.4"
  }
}
